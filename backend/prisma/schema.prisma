// backend/prisma/schema.prisma
datasource db {
  provider = "sqlite"
}

generator client {
  provider = "prisma-client-js"
  
}

model Url {
  id          String   @id
  originalUrl String
  createdAt   DateTime @default(now())

  @@index([id])
}
