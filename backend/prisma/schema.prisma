datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model Url {
  id          String   @id
  originalUrl String
  createdAt   DateTime @default(now())

  @@index([id])
}

