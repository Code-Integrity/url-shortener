datasource db {
  provider = "postgresql" // ★ sqlite から postgresql に変更

}

generator client {
  provider = "prisma-client-js"
  // outputは先ほど削除したままでOKです
}

model Url {
  id          String   @id
  originalUrl String
  createdAt   DateTime @default(now())

  @@index([id])
}

